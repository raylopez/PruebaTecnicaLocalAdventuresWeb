import { NumberPadZeroPipe } from './number-pad-zero-pipe';

describe('NumberPadZeroPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberPadZeroPipe();
    expect(pipe).toBeTruthy();
  });
});
